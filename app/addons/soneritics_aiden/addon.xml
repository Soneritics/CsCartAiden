<?xml version="1.0"?>
<addon scheme="3.0">
    <id>soneritics_aiden</id>
    <name>Soneritics Aiden</name>
    <version>1.0</version>
    <default_language>en</default_language>
    <priority>1000</priority>
    <status>active</status>
    <has_icon>Y</has_icon>
    <queries>
        <item>
            CREATE TABLE IF NOT EXISTS ?:soneritics_aiden(
                `category_id` MEDIUMINT(8) NOT NULL,
                `advisor_id` VARCHAR(100) NOT NULL DEFAULT '',
                `advisor_type` VARCHAR(255) NOT NULL DEFAULT '',
                PRIMARY KEY (`category_id`)
            )
        </item>
        <!-- For historic reference, do not delete this table! <item for="uninstall">DROP TABLE IF EXISTS ?:soneritics_aiden</item>-->
    </queries>
</addon>
